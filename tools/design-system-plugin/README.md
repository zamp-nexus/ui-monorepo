# design-system-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build design-system-plugin` to build the library.

## Running unit tests

Run `nx test design-system-plugin` to execute the unit tests via [Vitest](https://vitest.dev/).
