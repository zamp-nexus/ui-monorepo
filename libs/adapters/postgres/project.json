{
  "name": "postgres",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/adapters/postgres/src/zentra_adapter_postgres",
  "targets": {
    "lock": {
      "executor": "@nxlv/python:lock",
      "options": {
        "update": false
      }
    },
    "sync": {
      "executor": "@nxlv/python:sync",
      "options": {}
    },
    "add": {
      "executor": "@nxlv/python:add",
      "options": {}
    },
    "update": {
      "executor": "@nxlv/python:update",
      "options": {}
    },
    "remove": {
      "executor": "@nxlv/python:remove",
      "options": {}
    },
    "build": {
      "executor": "@nxlv/python:build",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "outputPath": "{projectRoot}/dist",
        "publish": false,
        "lockedVersions": true,
        "bundleLocalDependencies": true
      },
      "cache": true
    },
    "lint": {
      "executor": "@nxlv/python:run-commands",
      "outputs": [],
      "options": {
        "command": "uv run ruff check libs/adapters/postgres/src libs/adapters/postgres/tests && uv run lint-imports --config .importlinter",
        "cwd": "."
      },
      "cache": true
    },
    "format": {
      "executor": "@nxlv/python:ruff-format",
      "outputs": [],
      "options": {
        "filePatterns": ["src", "tests"]
      },
      "cache": true
    },
    "test": {
      "executor": "@nxlv/python:run-commands",
      "inputs": [
        "default",
        { "env": "TEST_DATABASE_OWNER_URL" },
        { "env": "TEST_DATABASE_RUNTIME_URL" }
      ],
      "outputs": [
        "{workspaceRoot}/reports/{projectRoot}/unittests",
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "command": "uv run pytest tests/",
        "cwd": "{projectRoot}"
      },
      "cache": true
    },
    "migrate": {
      "executor": "@nxlv/python:run-commands",
      "options": {
        "command": "uv run alembic -c libs/adapters/postgres/alembic.ini upgrade head",
        "cwd": "."
      }
    },
    "bootstrap-identity": {
      "executor": "@nxlv/python:run-commands",
      "options": {
        "command": "uv run python -m zentra_adapter_postgres.bootstrap",
        "cwd": "."
      }
    },
    "install": {
      "executor": "@nxlv/python:install",
      "options": {
        "silent": false,
        "args": "",
        "verbose": false,
        "debug": false
      }
    }
  },
  "tags": ["layer:adapter", "platform:python", "scope:core"],
  "release": {
    "version": {
      "versionActions": "@nxlv/python/release/version-actions"
    }
  }
}
