[tool.coverage.run]
branch = true
source = [ "src" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../../coverage/libs/domain/analysis_run/html'
 --cov-report xml:'../../../coverage/libs/domain/analysis_run/coverage.xml'
 --html='../../../reports/libs/domain/analysis_run/unittests/html/index.html'
 --junitxml='../../../reports/libs/domain/analysis_run/unittests/junit.xml'
"""

[project]
name = "zentra-domain-analysis-run"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.13,<3.14"
readme = 'README.md'
dependencies = [
  "zentra-domain-agent-execution",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []

[tool.uv.sources]
zentra-domain-agent-execution = { workspace = true }
