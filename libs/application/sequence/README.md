# zentra-application-sequence

Application service for the Sequence context: lists a Dataset Workspace's
Sequences, renders one Sequence's persisted lineage as a read model, previews
a Prepared Table, and creates a new Sequence (the manual creation path).

See `libs/domain/sequence/CONTEXT.md` for the domain vocabulary.
